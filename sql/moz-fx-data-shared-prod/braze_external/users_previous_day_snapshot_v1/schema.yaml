fields:
- name: external_id
  type: STRING
  mode: NULLABLE
  description: The Braze external identifier for this user, corresponding to their Basket email_id (UUID). Unique per user and used as the primary join key across
    all Braze-derived tables.
- name: email
  type: STRING
  mode: NULLABLE
  description: The user's primary email address as stored in CTMS, in lowercase. Users on the marketing suppression list or who have opted out of all email are excluded.
- name: email_subscribe
  type: STRING
  mode: NULLABLE
  description: The user's email subscription status for Braze. Values are 'opted_in' (double opt-in confirmed) or 'subscribed' (single opt-in, not yet double opt-in).
- name: mailing_country
  type: STRING
  mode: NULLABLE
  description: ISO 3166-1 alpha-2 country code (lowercase) for the user's mailing country as recorded in CTMS (e.g. 'us', 'de'). Null for approximately 36% of users.
- name: basket_token
  type: STRING
  mode: NULLABLE
  description: UUID token identifying the user in Mozilla's Basket email subscription service. Used as an alternative identifier for newsletter management.
- name: email_lang
  type: STRING
  mode: NULLABLE
  description: The preferred language for email communications as recorded in CTMS (e.g. 'en', 'fr', 'de'). Stored in lowercase.
- name: fxa_id_sha256
  type: STRING
  mode: NULLABLE
  description: SHA-256 hash of the user's Firefox Account (FxA) UID. Null for approximately 7.5% of users who have no linked FxA account.
- name: has_fxa
  type: BOOLEAN
  mode: NULLABLE
  description: Whether the user has a linked, non-deleted Firefox Account. True for approximately 92.5% of users; false for users with no associated FxA.
- name: fxa_primary_email
  type: STRING
  mode: NULLABLE
  description: The primary email address of the user's Firefox Account (may differ from the CTMS email). Null if the user has no linked FxA.
- name: fxa_lang
  type: STRING
  mode: NULLABLE
  description: The browser Accept-Language header string from the time of FxA account creation (e.g. 'en-us,en;q=0.5'). Null for approximately 47% of users.
- name: fxa_first_service
  type: STRING
  mode: NULLABLE
  description: The first Mozilla service the user signed into with their Firefox Account (e.g. 'sync', 'fenix', 'pocket-web'). Null for approximately 49% of users.
- name: fxa_created_at
  type: TIMESTAMP
  mode: NULLABLE
  description: Timestamp when the user's Firefox Account was created. Null for approximately 7.6% of users who do not have a linked FxA.
- name: create_timestamp
  type: TIMESTAMP
  mode: NULLABLE
  description: Timestamp when this user's record was first created in CTMS.
- name: update_timestamp
  type: TIMESTAMP
  mode: NULLABLE
  description: Timestamp when this user's record was most recently updated in CTMS.
